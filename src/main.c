#include <stdio.h>
#include <string.h>
#include <unistd.h>
#include <limits.h>
#include <sqlite3.h>
#include "data/connectData.h"
#include "data/directProject.h"
#include <gtk/gtk.h>
int main(int argc, char *argv[]) {
   /* GtkWidget *window;
    GtkWidget *button;
    GtkWidget *vbox;
    GtkWidget *label;

    gtk_init(&argc, &argv);

    // Tạo một cửa sổ
    window = gtk_window_new(GTK_WINDOW_TOPLEVEL);
    gtk_window_set_title(GTK_WINDOW(window), "Music Streaming App");
    gtk_window_set_default_size(GTK_WINDOW(window), 300, 200);
    gtk_container_set_border_width(GTK_CONTAINER(window), 10);

    // Tạo một hộp đựng các widget
    vbox = gtk_vbox_new(FALSE, 5);
    gtk_container_add(GTK_CONTAINER(window), vbox);

    // Tạo một nhãn
    label = gtk_label_new("Welcome to Music Streaming App!");
    gtk_box_pack_start(GTK_BOX(vbox), label, FALSE, FALSE, 0);

    // Tạo một nút bấm
    button = gtk_button_new_with_label("Play");
    gtk_box_pack_start(GTK_BOX(vbox), button, FALSE, FALSE, 0);

    // Kết thúc chương trình khi người dùng đóng cửa sổ
    g_signal_connect(G_OBJECT(window), "destroy", G_CALLBACK(gtk_main_quit), NULL);

    // Hiển thị cửa sổ và các widget bên trong
    gtk_widget_show_all(window);

    // Chạy vòng lặp chính của GTK
    gtk_main();

    return 0;*/

}